/** assets/capitals/*.jpg are named after each faction's capital art, not always the faction id itself (arabia-league's is just "arabia"). */
export const CAPITAL_IMAGE_IDS: Readonly<Record<string, string>> = {
  'arabia-league': 'arabia',
};
