/** Extra vertical gap (map-unit px) below a sea zone marker before unit icons start. */
export const SEA_ZONE_ICON_CLEARANCE_PX = 3;

/** Unit icon click/hover hit radius, as a fraction of its on-screen size (slightly smaller than the icon itself). */
export const UNIT_ICON_HIT_RADIUS_FACTOR = 0.7;
